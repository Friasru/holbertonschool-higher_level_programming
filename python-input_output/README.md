# Python - Input/Output

## Input/Output allows programs to receive data and display results. Input is commonly handled with the `input()` function, while output is displayed using the `print()` function. Python also supports reading from and writing to files, making it easy to store and process data.
